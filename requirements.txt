# Simplified requirements for Railway deployment (without Selenium initially)
Flask==3.0.0
Werkzeug==3.0.1
Pillow==10.1.0
openpyxl==3.1.2
gunicorn==21.2.0
python-dotenv==1.0.0

# Selenium dependencies (add back after basic deployment works)
# selenium==4.15.2
# webdriver-manager==4.0.1